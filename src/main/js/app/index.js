import React, { Component } from 'react';
import { BarChart } from '../charts/bar'

export class App extends Component {
    render() {
        return (
            <div>
                <BarChart />
            </div>
        )
    }
}